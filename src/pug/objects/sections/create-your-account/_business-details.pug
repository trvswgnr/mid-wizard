.subsection#business_details(data-title="Business Details")
  fieldset(data-title="Currencies")
    .guide-text
      h3 Let's talk money.
      p List all the currencies that you use for processing transactions.
    .field
      label(for="currencies") Select currencies you accept:
      select.js-currency-select#currencies(name="currencies" placeholder="Currencies" multiple)
        each currency in CURRENCIES
          option(value=currency.code)= `${currency.name} (${currency.symbol})`
    .controller-wrapper
      button.get-next-view Continue

  fieldset(data-title="Offers")
    .guide-text
      h3 What types of billing plans do you offer?
      p There’s more than one way to pay with a credit card … which do you utilize?
    .field
      label.hidden-review Select all the offer types you may accept from the choices below.
      .pills.pills--checkboxes
        input#offers_free(type="checkbox" class="pills__input" name="offers" value="Free Trial")
        label(for="offers_free") Customers sign up for a free or discounted trial and are billed automatically if they do not cancel within a pre-set timeframe.
        input#offers_single(type="checkbox" class="pills__input" name="offers" value="Single Order")
        label(for="offers_single") Customers order products or services through a traditional single transaction.
        input#offers_recurring(type="checkbox" class="pills__input" name="offers" value="Recurring")
        label(for="offers_recurring") Customers are billed automatically on a recurring basis until they cancel or modify their subscription.
        .controller-wrapper
          button.get-prev-view
          button.get-next-view Continue

  fieldset(data-title="Sales Methods")
    .guide-text
      h3 How do you accept credit card payments?
      p What sales methods do you use?
    .field
      label.hidden-review Select all the ways you accept payments.
      .pills.pills--checkboxes
        input#sales_methods_phone(type="checkbox" class="pills__input" name="sales_methods" value="Phone")
        label(for="sales_methods_phone") Customers give credit card information over the phone.
        input#sales_methods_web(type="checkbox" class="pills__input" name="sales_methods" value="Web")
        label(for="sales_methods_web") Customers fill out a form with credit card information on the internet.
        input#sales_methods_mail(type="checkbox" class="pills__input" name="sales_methods" value="Mail")
        label(for="sales_methods_mail") Customers send in their card information through the mail.
        .controller-wrapper
          button.get-prev-view
          button.get-next-view Continue

  fieldset(data-title="Number of Chargebacks")
    .guide-text
      h3 How extensive is your chargeback issue?
      p Estimate the number of chargebacks you receive per month.
    .block
      label Select a range from one of the options below:
      .pills.pills--radios.js-pills-select2
        input#number_chargebacks_10(type="radio" class="pills__input" name="number_of_chargebacks" value="Less than 10 Chargebacks" checked)
        input#number_chargebacks_25(type="radio" class="pills__input" name="number_of_chargebacks" value="10 to 25 Chargebacks")
        input#number_chargebacks_50(type="radio" class="pills__input" name="number_of_chargebacks" value="25 to 50 Chargebacks")
        input#number_chargebacks_100(type="radio" class="pills__input" name="number_of_chargebacks" value="50 to 100 Chargebacks")
        input#number_chargebacks_250(type="radio" class="pills__input" name="number_of_chargebacks" value="100 to 250 Chargebacks")
        input#number_chargebacks_500(type="radio" class="pills__input" name="number_of_chargebacks" value="250 to 500 Chargebacks")
        input#number_chargebacks_more(type="radio" class="pills__input" name="number_of_chargebacks" value="500+ Chargebacks")
      .controller-wrapper
        button.get-prev-view
        button.review-subsection Review
